

import devCredentials from './_dev.credentials'
import prodCredentials from './_prod.credentials'

// let credentials = prodCredentials;
let credentials = devCredentials;

export default credentials
